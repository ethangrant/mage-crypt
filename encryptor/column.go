package encryptor

import (
	// "database/sql"
	// "errors"
	"fmt"
	// "strconv"
	// "github.com/ethangrant/mage-crypt/cfg"
	// "github.com/ethangrant/mage-crypt/cipher"
	// "github.com/ethangrant/mage-crypt/model"
)

func (e *encryptor) Column(table string, column string) error {
	fmt.Println("Column command executed")
	fmt.Println(e.EnvPath)

	// query table for encrypted values under the provided column

	// decrypt and re-encrypt the results

	// query to insert batch update re-encrypted values

	// provide output of changes

	return nil
}
